# Resource directory

ゲーム実行時に読み込むアセットを種類と用途で分けています。

- `audio/`: BGM・効果音などの音声
- `models/<name>/`: モデルごとのOBJ・MTL・編集元データ
- `shaders/`: HLSLシェーダーと共有インクルード
- `textures/`: 複数のモデルや描画機能から共有する画像

モデルに専用テクスチャを追加する場合はモデル自身のフォルダへ、複数モデルで共有するテクスチャは `textures/` へ配置してください。MTL内の相対パスはMTLファイルが置かれたフォルダを基準にします。
